meta {
  name: 4. Renommer
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/projects/{{projectId}}/name
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Salon (etage)"
  }
}
