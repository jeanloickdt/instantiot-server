meta {
  name: 5. Update Layout
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/projects/{{projectId}}/layout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "layoutJson": "{\"widgets\":[{\"id\":\"w1\",\"x\":0,\"y\":0}]}"
  }
}
