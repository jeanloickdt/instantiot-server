meta {
  name: 4. Renommer
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/devices/{{deviceId}}/name
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "ESP32 Garage (v2)"
  }
}
