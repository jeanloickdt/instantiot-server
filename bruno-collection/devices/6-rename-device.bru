meta {
  name: 6. Rename Device
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/api/devices/{{deviceId}}/name
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "ESP32 Living Room"
  }
}
