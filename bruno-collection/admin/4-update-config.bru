meta {
  name: 4. Update Server Config
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/admin/config
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "httpPort": 8080,
    "tcpPort": 9001,
    "serverDisplayName": "InstantIoT Server"
  }
}
