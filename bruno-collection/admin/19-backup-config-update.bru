meta {
  name: Modifier les reglages de sauvegarde
  type: http
  seq: 19
}

patch {
  url: {{baseUrl}}/api/admin/backup/config
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "enabled": true,
    "intervalHours": 24,
    "retentionCount": 30
  }
}
