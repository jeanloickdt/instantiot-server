meta {
  name: 13. Reset User Password
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/api/admin/users/{{userId}}/reset-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "newPassword": "temporary-password-123"
  }
}
