meta {
  name: 15. Registration Config — Update
  type: http
  seq: 15
}

patch {
  url: {{baseUrl}}/api/admin/registration/config
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "open": true
  }
}
