meta {
  name: Ouvrir / fermer l'inscription
  type: http
  seq: 15
}

patch {
  url: {{baseUrl}}/api/admin/registration/config
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "open": true
  }
}

docs {
  A chaud. Ferme par defaut — on l'ouvre le temps d'enroler, puis on
  referme.
}
