meta {
  name: Reinitialiser un mot de passe
  type: http
  seq: 17
}

post {
  url: {{baseUrl}}/api/admin/users/{{userId}}/reset-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "newPassword": "provisoire-8-car"
  }
}

docs {
  Deconnecte l'utilisateur de tous ses appareils. A lui communiquer
  hors ligne.
}
