meta {
  name: 8. Backup Config — Update
  type: http
  seq: 8
}

patch {
  url: {{baseUrl}}/api/admin/backup/config
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "enabled": true,
    "intervalHours": 24,
    "retentionCount": 7
  }
}
