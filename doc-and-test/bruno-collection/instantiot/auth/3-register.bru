meta {
  name: 3. Register
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/register
  body: json
  auth: none
}

body:json {
  {
    "username": "testuser",
    "password": "monpassword123"
  }
}

script:post-response {
  if (res.status === 201) {
    bru.setVar("token", res.body.token);
  }
}
